# 依赖由 pyproject.toml 统一声明，此文件作为便捷入口。
# - 运行依赖：pyproject.toml `[project].dependencies`
# - 可选依赖：pyproject.toml `[project.optional-dependencies]` (browser / web / test / dev)
#
# 常用安装方式：
#   pip install -e .              # 仅运行依赖
#   pip install -e .[web]         # 启用 streamlit Web UI（dayu-web）
#   pip install -e .[test]        # 运行 + 测试
#   pip install -e .[test,dev,browser,web] -c constraints/lock-<platform>-py311.txt
#                                 # 受控全量开发环境

-e .[test,dev,browser,web]
